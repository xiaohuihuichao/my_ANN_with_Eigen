CMAKE_MINIMUM_REQUIRED( VERSION 3.6.2 )

PROJECT( demo )

AUX_SOURCE_DIRECTORY( . DIR_SRC_DEMO )

LINK_DIRECTORIES( ../build/Release )

ADD_EXECUTABLE( demo ${DIR_SRC_DEMO} )

TARGET_LINK_LIBRARIES( demo libnet )