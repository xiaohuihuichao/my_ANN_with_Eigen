CMAKE_MINIMUM_REQUIRED( VERSION 3.6.2 )

PROJECT( Net )

AUX_SOURCE_DIRECTORY( src DIR_SRC_NET )

INCLUDE_DIRECTORIES( include ../eigen_v3.3.5/Eigen )

ADD_LIBRARY( libnet ${DIR_SRC_NET} )

ADD_SUBDIRECTORY( demo )